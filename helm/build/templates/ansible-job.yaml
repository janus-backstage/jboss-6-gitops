apiVersion: tower.ansible.com/v1alpha1
kind: AnsibleJob
metadata:
  name: example-job-jboss-6
  namespace: aap
spec:
  connection_secret: controller-access
  workflow_template_name: Demo Job Template
